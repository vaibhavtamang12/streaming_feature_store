DB_CONFIG = {
    "host": "postgres",
    "database": "feature_store",
    "user": "postgres",
    "password": "postgres"
}

MLFLOW_TRACKING_URI = "http://mlflow:5000"

# Training window (last N minutes)
TRAINING_WINDOW_MINUTES = 60